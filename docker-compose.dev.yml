# Dev overlay — adds cargo build capabilities to the omniagent container
# and binds source mounts for the dashboard.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  omniagent:
    build:
      context: /opt/workspace/omniagent
      dockerfile: Dockerfile.dev
    image: omniagent-dev:latest
    env_file: []
    environment:
      CARGO_TARGET_DIR: /target
      # Resolved from .env — cargo needs this for sql_forge! compile-time verification
      DATABASE_URL: ${DATABASE_URL}
    volumes:
      - /opt/workspace/omniagent:/app:rw
      - cargo-registry:/usr/local/cargo/registry
      - cargo-target:/target

  dashboard:
    build:
      context: /opt/workspace/omni-dashboard/repo
      dockerfile: Dockerfile
    command: ["node", "/opt/repo/server-dist/index.js"]
    volumes:
      - /opt/workspace/omni-dashboard/repo:/opt/repo:rw
      - /opt/workspace/omni-dashboard/repo/node_modules:/app/node_modules:ro

networks:
  default:
    driver: bridge

volumes:
  cargo-registry:
  cargo-target:
