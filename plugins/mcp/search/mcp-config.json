{
  "servers": [
    {
      "name": "search",
      "transport": "stdio",
      "args": [],
      "timeout_secs": 60,
      "max_retries": 2,
      "allowed_tools": [
        "*"
      ],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}",
        "OMNI_DIR": "${OMNI_DIR:-/opt/data}",
        "RUST_LOG": "info"
      }
    }
  ]
}