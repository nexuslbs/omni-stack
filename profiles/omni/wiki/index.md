# OmniAgent Wiki

## Index

- **Skills** (in `profiles/omni/skills/`):
  - [Deploy Suite Debugging](./skills/deploy-suite-debugging.md): lessons from the actions-plugin saga — "tool not found" is an ordering/registration bug first; test incrementally (group → neighbors → full deploy) with self-contained tests; reuse prior-execution context (Smartness notes.md / context dumps / prior threads) instead of re-deriving state
  - [Live Smoke Toolbox](./skills/live-smoke-toolbox.md): live-smoke-test a NEW omniagent build against the isolated omnidev-toolbox stack (fresh pgvector DB + release binary from current HEAD) without touching the deployed stack — validated 2026-08-18 (kanban board-validation smoke)

- **Todo/**
  - [Hooks Implementation](./Todo/HooksImplementation.md): Plan of action for the event-driven Hooks system (thread_started/thread_finished/new_message) — counters, scopes, execution modes, loop protection, error isolation
  - [Workflow Implementation](./Todo/WorkflowImplementation.md): Plan of action for the role-based kanban workflow (executor/tester/reviewer) — schema, status machine, fail matrix, phases, test matrix
  - [Plan Normalization Implementation](./Todo/PlanNormalizationImplementation.md): Drop the legacy `planning_mode` field everywhere (kanban_tasks/threads columns, tasks.yml PlanningMode enum, API, dashboard) — single `plan` bool
  - [Default Channels Implementation](./Todo/DefaultChannelsImplementation.md): Default cli/schedule/hook/kanban channel settings (selects over existing channels); platform-less channel = cli; empty channel → fail-with-record
  - [Plugin Restart Endpoint](./Todo/PluginRestartEndpointImplementation.md): Fix `/api/plugins/{type}/{source}/{name}/restart` to dispatch by type (tool/platform/provider); make `enable` a no-op when already enabled
  - [Max Tokens + Truncation Retry](./Todo/MaxTokensTruncationRetryImplementation.md): Global `max_tokens_on_truncation` escalation — normal calls keep max_tokens 4096, truncated responses retry once with a larger budget + preserved reasoning, still truncates → fail fast
  - [Stop-Thread Surgical](./Todo/StopThreadSurgicalImplementation.md): POST /stop-thread/{id} must affect ONLY the target thread — no unconditional channel-handler cancel that orphans a different processing thread; stopped kanban thread clears its task's thread_status
  - [Deploy.py dev Run](./Todo/DeployPyDevRunImplementation.md): run `python3 deploy.py dev` in omni-deployer to successful completion, fixing issues mid-run; deploy must stop omnidev (fine) but NEVER omnistable (the agent's own stack) — fix STOP_TARGETS["omnideploy"] accordingly
  - [Kanban Status-Change Dispatch + /redispatch](./Todo/KanbanStatusChangeDispatchImplementation.md): status change dispatches the mapped workflow role (running→executor, testing→tester, review→reviewer); stale threads skipped first; new /redispatch endpoint recreates the role thread without changing status; dispatcher action simplified to move-to-running; startup recovery unified
  - [Cache-Friendly Compaction](./Todo/CacheFriendlyCompactionImplementation.md): Stable summary block reused verbatim so DeepSeek prefix caching survives compaction; align plugin budgets with core
  - [Kanban Boards](./Todo/KanbanBoardsImplementation.md): Boards concept — config/boards.yml (gated by presence), tasks belong to boards, invalid-board tasks ignored/failed, resolution order gains a Board layer (Workflow Role > Workflow > Kanban Task > Board > Channel > Global Settings), dashboard board selector + create/edit/delete + move-to-board; omnidev only
  - [External / Agnostic MCP Servers](./Todo/McpExternalServersImplementation.md): Make omniagent work with external standard MCP servers — add the 7 modelcontextprotocol Reference Servers (Everything, Fetch, Filesystem, Git, Memory, Sequential Thinking, Time) as remote MCPs with live tool-call verification; extend install API for Python (requirements.txt) / NodeJS (package.json) deps; external OS binaries live in the image; integration tests covering ≥1 tool per server
  - [Python Telegram Platform](./Todo/PythonTelegramPlatformImplementation.md): Python telegram platform plugin in omni-plugins — implement the platform protocol (initialize/configure/deliver/edit/delete + inbound polling) against the Telegram Bot API; test via a MOCK API (no real token — never reuse hermes' bot); integration tests G33; document what a real full test needs (@BotFather token for omniagent)
  - [SSH Plugin](./Todo/SshPluginImplementation.md): New builtin `ssh` tool plugin (plugins/tools/ssh/, mcp-server-ssh) — ssh_run/ssh_copy(+status) over the ssh/scp CLIs, keys/config from `{OMNI_DIR}/data/ssh/`, background-task integration; agnostic of use; remote-development workflow lives in a skill + template, not the plugin; G34 integration tests
  - [Subtasks Improvement](./Todo/SubtasksImprovement.md): Research spec — subtasks exist but are inert (manage_subtasks tool never registered, write tools disabled in allowed_tools, auto-create expects JSON steps while real plans are <plan> markdown, prompt never references them, enforcement would force-fail if they existed); Path A-lite: add real manage_subtasks tool, parse markdown plans, enable tools, prompt guidance, token-safe enforcement
  - [Paperclip Service](./Todo/PaperclipServiceImplementation.md): add paperclip service to compose (profiles paperclip + full-stack umbrella, pinned fixed GHCR sha) + integrate via the OFFICIAL @paperclipai/mcp-server as an external MCP plugin (30+ typed tools + paperclipApiRequest escape hatch) — rejected custom Python plugin (maintenance) and skill+fetch (boilerplate); thin wrapper = recommended; fallback = thin fetch plugin
  - [Hooks Event Meta + Event Object](./Todo/HooksEventMetaImplementation.md): hooks counter JSON gains top-level `meta` (last_thread/last_message = previous trigger's ids); every trigger delivers an event object (last_*, current_thread, current_message, channel, profile) — action mode receives it in tool args, agentic mode embeds it as JSON in the prompt; new no-channel/no-profile guard; keep hook-caused loop protection
  - [Old-Data Cleanup + Core Dispatcher](./Todo/CleanupAndCoreDispatcherImplementation.md): extend daily cleanup to delete old threads + kanban_history (existing delete_after_days, 0 = disable); move kanban dispatcher into core (in-process background loop, new kanban_dispatcher_interval setting default 15s, remove kanban_dispatcher from actions plugin)
  - [Action Plugin → omni-plugins (Python)](./Todo/ActionsPluginPythonOmniPluginsImplementation.md): port the remaining 3 action tools (hindsight_populator, relevance_indexer, setup_knowledge_pipeline) to a Python MCP plugin in omni-plugins/tools/actions/ (evolve without omniagent releases); remove the built-in Rust actions plugin from omniagent; wire remote in omni-stack (remote.yml + plugins.yml source: remote)
  - [Skills Gap: Display Bug + Hermes-Compatible Layout + Prompt Nudge](./Todo/SkillsGapImplementation.md): close the skills lifecycle loop — fix get_skills frontmatter display bug (`---` renders as description), align create_skill with Hermes SKILL.md layout + richer frontmatter, strengthen the prompt nudge so agents actually create/view skills (verified: 0 create_skill / 0 view_skill calls ever)
  - [Plugin Consolidation: prompt dual, telegram/hindsight out, search merge, generic omniagent-API tool](./Todo/PluginConsolidationImplementation.md): one consolidation pass — prompt stays Rust built-in with omni-plugins python port as experimental channel; telegram removed from core (python platform is real); hindsight moved to omni-plugins (Rust ok); search+query+metrics merged into one search plugin (tools kept, renamed search_*); fetch/skills/notes stay; cron+kanban merged into ONE generic core builtin omniagent-api tool (like builtin_wait-task) + DELETE /schedule/{id} + skill md doc
  - [Workflow Role Mode (agent/action) + Auto-Approve](./Todo/WorkflowRoleModeAutoApproveImplementation.md): per-role `mode: agent|action` in workflows.yml (default agent, mirroring hooks/schedule) — action-mode roles execute the actions.yml tool call instead of the agent loop (executor fail→blocked, tester fail→review, reviewer fail→blocked, interruption retries per normal); workflow-level `auto_approve` (no effective reviewer, review→done directly, review_on_fail forced false) + new `review_on_fail` flag (true → failed steps go to review instead of blocked); dashboard workflows page gains mode select (action select replaces template select in action mode), auto_approve + review_on_fail checkboxes (review_on_fail disabled/unchecked under auto_approve)
  - [Hooks: Wiki/Templates/Skills + Summaries](./Todo/HooksWikiSummariesImplementation.md) **(IMPLEMENTED 2026-08-18, omniagent `56b486b`)**: 2 agentic `thread_finished` hooks, count 10 — profile-scoped wiki/templates/skills maintenance (create/update/delete/merge wiki+skills, update templates only when REALLY valuable, NO template create/delete/merge, profile-scoped only) and channel-scoped summaries (write to existing `summaries` table via channel_id); core changes: per-scope-key counter meta fix + remove auto-summary `generate_summary` tool, add minimal save-summary tool; 2 new skills (wiki-maintenance, channel-summary)
  - [Kanban API Board Validation](./Todo/KanbanBoardValidationImplementation.md) **(IMPLEMENTED 2026-08-18, omniagent `9a7f8c0`)**: require `board` on POST/PATCH /kanban/tasks when boards.yml present — 400 on missing/unknown/cleared board; smoke-verified live via omnidev-toolbox (see live-smoke-toolbox skill)
  - [Channel Naming mm-kanban](./Todo/ChannelNamingMmKanbanImplementation.md): `$new <name>` must create a channel keyed `<name>` (mm-kanban), not `{platform}-{first8}` — prepare posts `$new mm-kanban` but handle_new_external (commands.rs:256) derives `mattermost-wkbugy5x`; boards.yml omnidev + channels.yml rename to `mm-kanban`
  - [Dashboard Board Workflow Display](./Todo/DashboardBoardWorkflowDisplayImplementation.md): dashboard shows each board's workflow
  - [Remove Channel cause](./Todo/RemoveChannelCauseImplementation.md): `cause` is not relevant for a channel
  - [Dispatcher Archived Filter](./Todo/DispatcherArchivedFilterImplementation.md): archived tasks must never be dispatched — the scan SQL (kanban_dispatch.rs:137-143) filters status='todo' only, so an archived task still in todo gets promoted (observed task_18cd0a6c7ef43217); add `archived = false` to the scan + tests
  - [Provider Relative Entrypoint](./Todo/ProviderRelativeEntrypointImplementation.md): provider entrypoints must resolve relative args against the plugin dir like platforms do
  - [Plugin omni_dir Config](./Todo/PluginOmniDirConfigImplementation.md): plugins resolve their data dir via an `omni_dir` config field
  - [Sub-Prompts Append](./Todo/SubPromptsAppendImplementation.md): running user thread appends pending same-context user prompts (same channel/profile/parent_id) into its next LLM prompt BEFORE compaction — pending thread marked skipped, sub_cause message with original_thread_id, cumulative char budget (sub_prompt_max_chars) + iteration-percent gate (sub_prompt_iteration_percent, default 50%, 0 disables) (default `$env:OMNI_DIR`) instead of hardcoded `/opt/omni` fallbacks — memory/actions server.py use `os.environ.get("OMNI_DIR", "/opt/omni")` ignoring the existing config_schema field; prompt has no config_schema and a `~/.omniagent` fallback; framework already injects config-as-env (apply_config_schema_defaults) (external/mod.rs:158-180) — plugins_env.rs provider path only does brittle prefix replacement and provider spawn sets no current_dir; noop-full plugin.json hardcodes /opt/omni/... which breaks with a different OMNI_DIR — drop it from channels.yml schema (ChannelDef), the Channel/CreateChannelParams/ChannelEntry structs, the API response, and all construction/validation sites; thread cause stays (selector + choose-board buttons); board create/edit workflow field becomes a SELECT of workflows.yml keys (fetchWorkflows) instead of free-text input — today the API accepts NULL board and the auto-dispatcher silently skips such tasks (observed 2026-08-18: task_18cd061e85394a52 sat in todo until PATCH set board:dev); reuse existing `boards_enabled` + `task_board` helpers; 400 on missing/unknown/cleared board
  - [Fail-Thread Routing (review_on_fail) + Double-Normalization Fix](./Todo/FailThreadRoutingReviewOnFailImplementation.md): fix `engine_transition` re-normalizing the already-normalized fail-thread step (F0 empty `workflow_step` → "invalid" → blocked; must re-run executor); wire `review_on_fail=true` into the fail matrix — tester F0 → review (reviewer decides), only the reviewer can send blocked (explicit fail or reviewer retry limit, plus skip), non-reviewer blocked/invalid/retry-limit → review; tester adds unit + integration tests for (1) go-to-executor and (2) tester-fail-without-target, each with the flag true and false
- **Reference/**
  - [Agent Guidance Architecture](./Reference/Agent-Guidance-Architecture.md): The 4-layer guidance model (memory → templates → skills → wiki), what goes where, and conventions
  - [Budget & Context Mechanics](./Reference/Budget-and-Context.md): Thread budget, compaction behavior, prompt context blocks, filesystem_read paging
  - [DeepSeek Prefix-Cache Misses](./Reference/DeepSeek-Prefix-Cache.md): Why cached_tokens froze at 7,424 (system-role upserts break the cache key) and the user-role fix (omniagent 9c5bb60) — 6.9% → 90%+ hit rate
  - [Deployment Checklist](./Reference/Deployment-Checklist.md): How to deploy services correctly via compose
  - [Omni-Deployer](./Reference/Omni-Deployer.md): The deploy harness — deploy.py dev pipeline (verified exit 0), noop-only rule, omnistable safety invariant, schema facts that bite tests, test-fix patterns
  - [Container Mount Map](./Reference/Container-Mount-Map.md): Volume mount mapping between host and container (omni-stack → /opt/omni, NOT /opt/data)
  - [Omniagent Mattermost Platform](./Reference/Omniagent-Mattermost-Platform.md): Mattermost platform architecture, setup, invariants, and recovery
  - [Verification & Review Requirements](./Reference/Verification-and-Review.md): Mandatory functional verification + review-before-commit for code deliverables (plugins, tools, services)
- **Log**
  - [log.md](./log.md): Change log

## Key Facts

- You have NO shell/terminal tool. All operations go through MCP tools.
- Docker operations: `docker_compose` MCP tool only.
- File operations: `filesystem_*` MCP tools only. `filesystem_read` supports
  offset/limit char-based paging; `filesystem_search` matches file names only.
- Port checking via `fetch_fetch` is unreliable from inside the container — use `docker_compose ps`.
- Container mount map: `/opt/workspace` == `/opt/workspace`; omni-stack == `/opt/omni`;
  omniagent == `/app`.
