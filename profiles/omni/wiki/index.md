# OmniAgent Wiki

## Index

- **Skills** (in `profiles/omni/skills/`):
  - [Deploy Suite Debugging](./skills/deploy-suite-debugging.md): lessons from the actions-plugin saga — "tool not found" is an ordering/registration bug first; test incrementally (group → neighbors → full deploy) with self-contained tests; reuse prior-execution context (Smartness notes.md / context dumps / prior threads) instead of re-deriving state
  - [Live Smoke Toolbox](./skills/live-smoke-toolbox.md): live-smoke-test a NEW omniagent build against the isolated omnidev-toolbox stack (fresh pgvector DB + release binary from current HEAD) without touching the deployed stack — validated 2026-08-18 (kanban board-validation smoke)
  - [Wiki](./skills/wiki.md) **(NEW 2026-08-19)**: the wiki is a data source, not a plugin — Karpathy method (index.md catalog first, log.md append-only log, Memory/ Reference/ Todo/ pages), Obsidian format, when-to-use table (search_wiki text vs filesystem_search names vs filesystem_read of index.md), complete "record a new decision" worked example with real filesystem tools; vectorize_wiki stays OFF

- **Todo/**
  - [Hooks Implementation](./Todo/HooksImplementation.md) **(IMPLEMENTED 2026-08-18)**: event-driven hooks (thread_started/thread_finished/new_message) — counters, scopes, execution modes, loop protection, error isolation; hooks defined in `config/tasks.yml`; see HooksWikiSummariesImplementation.md
  - [Workflow Implementation](./Todo/WorkflowImplementation.md) **(IMPLEMENTED 2026-08-17)**: role-based kanban workflow (executor/tester/reviewer) — schema, status machine, fail matrix, phases; workflows defined in `config/workflows.yml`; see WorkflowRoleModeAutoApproveImplementation.md
  - [Plan Normalization Implementation](./Todo/PlanNormalizationImplementation.md) **(IMPLEMENTED 2026-08-13)**: Drop the legacy `planning_mode` field everywhere (kanban_tasks/threads columns, tasks.yml PlanningMode enum, API, dashboard) — single `plan` bool
  - [Default Channels Implementation](./Todo/DefaultChannelsImplementation.md) **(IMPLEMENTED 2026-08-13)**: Default cli/schedule/hook/kanban channel settings (selects over existing channels); platform-less channel = cli; empty channel → fail-with-record
  - [Plugin Restart Endpoint](./Todo/PluginRestartEndpointImplementation.md) **(IMPLEMENTED 2026-08-13, omniagent `0245407`)**: `/api/plugins/{type}/{source}/{name}/restart` dispatches by type (tool/platform/provider); `enable` is a no-op when already enabled
  - [Max Tokens + Truncation Retry](./Todo/MaxTokensTruncationRetryImplementation.md) **(IMPLEMENTED 2026-08-17)**: Global `max_tokens_on_truncation` escalation — normal calls keep max_tokens 4096, truncated responses retry once with a larger budget + preserved reasoning, still truncates → fail fast
  - [Stop-Thread Surgical](./Todo/StopThreadSurgicalImplementation.md) **(IMPLEMENTED 2026-08-14)**: POST /stop-thread/{id} must affect ONLY the target thread — no unconditional channel-handler cancel that orphans a different processing thread; stopped kanban thread clears its task's thread_status
  - [Deploy.py dev Run](./Todo/DeployPyDevRunImplementation.md): run `python3 deploy.py dev` in omni-deployer to successful completion, fixing issues mid-run; deploy must stop omnidev (fine) but NEVER omnistable (the agent's own stack) — fix STOP_TARGETS["omnideploy"] accordingly
  - [Omnidev Chain Run](./Todo/OmnidevChainRunImplementation.md) **(DONE 2026-08-22)**: dev-executor task on the omnidev board (workflow dev-executor, auto_approve) running the 4-step omnidev chain — `python3 omnidev.py setup` → `test` → `agent` → `prepare` — all 4 exit 0 (setup 554s / test 302s 149-pass / agent 597 / prepare 14s); omnistable never stopped (STOP_TARGETS["omnidev"] = ["omnideploy"]); mid-task provider failover opencode-go → deepseek (429 quota). Re-run GREEN again 2026-08-22 thread 80 (first attempt thread 79 died pre-work on provider 429 — see Omni-Deployer.md)
  - [Deploy Hybrid Token Budget](./Todo/DeployHybridTokenBudgetImplementation.md) **(DISPATCHED 2026-08-22)**: dev-executor task on the omnidev board to run `deploy.py hybrid` to green with HARD token caps (cache hit ≤ 2M, cache miss ≤ 200K on the executor thread); Hermes enforces the gates and iterates fixes (agent prompts/skills/wiki/prompt-generate/compact-messages/tools/templates) + re-runs deploy.py dev until caps respected; hybrid stops neither omnistable nor omnidev (a62dc71). First execution attempt (thread 81) failed pre-work on provider 429
  - [Kanban Status-Change Dispatch + /redispatch](./Todo/KanbanStatusChangeDispatchImplementation.md): status change dispatches the mapped workflow role (running→executor, testing→tester, review→reviewer); stale threads skipped first; new /redispatch endpoint recreates the role thread without changing status; dispatcher action simplified to move-to-running; startup recovery unified
  - [Cache-Friendly Compaction](./Todo/CacheFriendlyCompactionImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `e8239a0`+`f29e9a4`)**: stable summary block reused verbatim so prefix caching survives compaction — folded into the compact+prune task
  - [Kanban Boards](./Todo/KanbanBoardsImplementation.md) **(IMPLEMENTED 2026-08-20)**: Boards concept — config/boards.yml (gated by presence), tasks belong to boards, board required on create/edit when present, resolution order gains a Board layer (Workflow Role > Workflow > Kanban Task > Board > Channel > Global Settings), dashboard board selector + create/edit/delete + move-to-board
  - [External / Agnostic MCP Servers](./Todo/McpExternalServersImplementation.md) **(IMPLEMENTED 2026-08-16)**: Make omniagent work with external standard MCP servers — add the 7 modelcontextprotocol Reference Servers (Everything, Fetch, Filesystem, Git, Memory, Sequential Thinking, Time) as remote MCPs with live tool-call verification; extend install API for Python (requirements.txt) / NodeJS (package.json) deps; external OS binaries live in the image; integration tests covering ≥1 tool per server
  - [Python Telegram Platform](./Todo/PythonTelegramPlatformImplementation.md) **(WIRED 2026-08-17)**: Python telegram platform plugin in omni-plugins — implement the platform protocol (initialize/configure/deliver/edit/delete + inbound polling) against the Telegram Bot API; test via a MOCK API (no real token — never reuse hermes' bot); integration tests G33; document what a real full test needs (@BotFather token for omniagent)
  - [SSH Plugin](./Todo/SshPluginImplementation.md) **(IMPLEMENTED, omniagent `bccfd2a`)**: builtin `ssh` tool plugin (plugins/tools/ssh/, mcp-server-ssh) — ssh_run/ssh_copy/ssh_status, keys/config from `{OMNI_DIR}/data/ssh/`; remote-development workflow lives in a skill + template
  - [Subtasks Improvement](./Todo/SubtasksImprovement.md) **(IMPLEMENTED, omniagent `14e832f`+`d601dd5`)**: real manage_subtasks tool + markdown-plan auto-create + token-safe enforcement
  - [Paperclip Service](./Todo/PaperclipServiceImplementation.md) **(IMPLEMENTED 2026-08-16)**: add paperclip service to compose (profiles paperclip + full-stack umbrella, pinned fixed GHCR sha) + integrate via the OFFICIAL @paperclipai/mcp-server as an external MCP plugin (30+ typed tools + paperclipApiRequest escape hatch) — rejected custom Python plugin (maintenance) and skill+fetch (boilerplate); thin wrapper = recommended; fallback = thin fetch plugin
  - [Hooks Event Meta + Event Object](./Todo/HooksEventMetaImplementation.md): hooks counter JSON gains top-level `meta` (last_thread/last_message = previous trigger's ids); every trigger delivers an event object (last_*, current_thread, current_message, channel, profile) — action mode receives it in tool args, agentic mode embeds it as JSON in the prompt; new no-channel/no-profile guard; keep hook-caused loop protection
  - [Old-Data Cleanup + Core Dispatcher](./Todo/CleanupAndCoreDispatcherImplementation.md) **(IMPLEMENTED 2026-08-17)**: extend daily cleanup to delete old threads + kanban_history (existing delete_after_days, 0 = disable); move kanban dispatcher into core (in-process background loop, new kanban_dispatcher_interval setting default 15s, remove kanban_dispatcher from actions plugin)
  - [Action Plugin → omni-plugins (Python)](./Todo/ActionsPluginPythonOmniPluginsImplementation.md) **(IMPLEMENTED 2026-08-17, omniagent `89c08f3`/`1285b50`)**: built-in Rust actions plugin removed; Python plugin in omni-plugins/tools/actions/, wired via omni-stack remote.yml (path: tools/actions); deploy-env registration under verification
  - [Skills Gap: Display Bug + Hermes-Compatible Layout + Prompt Nudge](./Todo/SkillsGapImplementation.md) **(IMPLEMENTED 2026-08-17)**: close the skills lifecycle loop — fix get_skills frontmatter display bug (`---` renders as description), align create_skill with Hermes SKILL.md layout + richer frontmatter, strengthen the prompt nudge so agents actually create/view skills (verified: 0 create_skill / 0 view_skill calls ever)
  - [Plugin Consolidation: prompt dual, telegram/hindsight out, search merge, generic omniagent-API tool](./Todo/PluginConsolidationImplementation.md) **(IMPLEMENTED 2026-08-17)**: one consolidation pass — prompt stays Rust built-in with omni-plugins python port as experimental channel; telegram removed from core (python platform is real); hindsight moved to omni-plugins (Rust ok); search+query+metrics merged into one search plugin (tools kept, renamed search_*); fetch/skills/notes stay; cron+kanban merged into ONE generic core builtin omniagent-api tool (like builtin_wait-task) + DELETE /schedule/{id} + skill md doc
  - [Workflow Role Mode (agent/action) + Auto-Approve](./Todo/WorkflowRoleModeAutoApproveImplementation.md): per-role `mode: agent|action` in workflows.yml (default agent, mirroring hooks/schedule) — action-mode roles execute the actions.yml tool call instead of the agent loop (executor fail→blocked, tester fail→review, reviewer fail→blocked, interruption retries per normal); workflow-level `auto_approve` (no effective reviewer, review→done directly, review_on_fail forced false) + new `review_on_fail` flag (true → failed steps go to review instead of blocked); dashboard workflows page gains mode select (action select replaces template select in action mode), auto_approve + review_on_fail checkboxes (review_on_fail disabled/unchecked under auto_approve)
  - [Hooks: Wiki/Templates/Skills + Summaries](./Todo/HooksWikiSummariesImplementation.md) **(IMPLEMENTED 2026-08-18, omniagent `56b486b`)**: 2 agentic `thread_finished` hooks, count 10 — profile-scoped wiki/templates/skills maintenance (create/update/delete/merge wiki+skills, update templates only when REALLY valuable, NO template create/delete/merge, profile-scoped only) and channel-scoped summaries (write to existing `summaries` table via channel_id); core changes: per-scope-key counter meta fix + remove auto-summary `generate_summary` tool, add minimal save-summary tool; 2 new skills (wiki-maintenance, channel-summary)
  - [Kanban API Board Validation](./Todo/KanbanBoardValidationImplementation.md) **(IMPLEMENTED 2026-08-18, omniagent `9a7f8c0`)**: require `board` on POST/PATCH /kanban/tasks when boards.yml present — 400 on missing/unknown/cleared board; smoke-verified live via omnidev-toolbox (see live-smoke-toolbox skill)
  - [Remove SOUL.md Support + Single Root MEMORY.md](./Todo/SoulRemovalImplementation.md) **(BACKLOG 2026-08-23, omnidev board — NOT dispatched until requested)**: only MEMORY.md (may include SOUL.md content); single `MEMORY.md` at profile ROOT (`profiles/omni/MEMORY.md`), not `memories/`; transitionally keep BOTH (copy, same content) until next release; remove soul from dashboard Memory page (no soul section), remove `/memory/*/soul` (USER.md) API type, prompt plugin stops returning `soul` field, omniagent stops expecting it
  - [Channel Naming mm-kanban](./Todo/ChannelNamingMmKanbanImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `b9058c8` + omni-stack `8f5b8f5`)**: `$new <name>` must create a channel keyed `<name>` (mm-kanban), not `{platform}-{first8}` — prepare posts `$new mm-kanban` but handle_new_external (commands.rs:256) derives `mattermost-wkbugy5x`; user-corrected design: OPTIONAL first arg — upsert-by-name verbatim, bare `$new` keeps `{platform}-{first8}`; boards.yml omnidev + channels.yml rename to `mm-kanban` (LIVE: threads ≥27 have channel_id `mm-kanban`)
  - [Dashboard Board Workflow Display](./Todo/DashboardBoardWorkflowDisplayImplementation.md) **(IMPLEMENTED 2026-08-18)**: dashboard shows each board's workflow
  - [Remove Channel cause](./Todo/RemoveChannelCauseImplementation.md) **(IMPLEMENTED 2026-08-19)**: `cause` is not relevant for a channel
  - [Dispatcher Archived Filter](./Todo/DispatcherArchivedFilterImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `070e8f4`)**: archived tasks must never be dispatched — the scan SQL (kanban_dispatch.rs:137-143) filters status='todo' only, so an archived task still in todo gets promoted (observed task_18cd0a6c7ef43217); fix = `AND archived = false` + Rust backstop `scan_row_eligible` + `create_kanban_step_thread` returns Ok(None) for archived (backstops ALL dispatch paths); regression test added
  - [Provider Relative Entrypoint](./Todo/ProviderRelativeEntrypointImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `d9f323d` + omni-plugins `1659583`)**: provider entrypoints must resolve relative args against the plugin dir like platforms do (+ provider spawn current_dir); noop-full plugin.json arg → `client.py`
  - [Plugin omni_dir Config](./Todo/PluginOmniDirConfigImplementation.md) **(IMPLEMENTED 2026-08-19, omni-plugins `19bb5bc` + omni-deployer `dedc5e3` GROUP 42)**: plugins resolve their data dir via an `omni_dir` config field (default `$env:OMNI_DIR`) instead of hardcoded `/opt/omni` fallbacks — memory/actions/prompt server.py config-first + `_fail_omni_dir()` RuntimeError; prompt plugin.json gains omni_dir config_schema
  - [Sub-Prompts Append](./Todo/SubPromptsAppendImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `5d17bd3` + omni-stack `7268d2e` + omni-deployer `20a3778` GROUP 43 4/4)**: running user thread appends pending same-context user prompts (same channel/profile/parent_id) into its next LLM prompt BEFORE compaction — pending thread marked skipped, sub_cause message with original_thread_id (new `messages.original_thread_id` column), cumulative char budget (sub_prompt_max_chars 4000) + iteration-percent gate (sub_prompt_iteration_percent 50%, 0 disables)
  - [Builtin omniagent-api + fetch allow_unsafe_methods](./Todo/OmniagentApiBuiltinImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `0ecb985` + omni-deployer `a287ffe` GROUP 44 3/3 + omni-stack `f85f9bb`)**: builtin `omniagent-api` tool hardened (30s timeout, non-2xx → tool error, base URL `http://localhost:8080` derived internally) + fetch plugin `allow_unsafe_methods` config (default false; enables POST via fetch for the plugin API) + Dockerfile ships api-reference.md → `/opt/omni/docs/api.md`; skill omniagent-api.md updated
  - [yml/API Field Naming Parity](./Todo/YamlApiFieldParityImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `9c52028`+`f147c52`, omni-dashboard `305199d`+`bc4e622`, omni-deployer `3833c0c`+`500e1a4`, omni-stack `f3ebfdf`)**: HTTP API field names unified to yml property names — `channel_id`/`channel_name`→`channel`, `workflow_id`→`workflow`, `schedule`(cron expr)→`cron`, `current_profile/provider/model`→bare; 10 src/server/*.rs files, NO DB migration (1:1 SQL mapping at the boundary); audit of 9 ymls (KEEP: `plan_mode`, `/stop/{channel_id}` path params, `schedule_task_id` DB-only, kanban history keys; CONSISTENT: actions/plugins/remote/settings/boards.yml); dashboard tsc fixes (threads.ts duplicate channel); skill omniagent-api.md uses `channel`
  - [Context Budget Unification (token-only)](./Todo/ContextBudgetUnificationImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `bf2af90` + omni-stack `451a461` + omni-deployer `3386e1d`)**: remove ALL char budgets; budgets are GLOBAL SETTINGS in core — `AgentConfig.token_budget_hard/soft` from settings keys `prompt_token_budget_hard/soft` (defaults soft **100000** / hard **500000** at both load sites + settings.rs SettingMeta), passed to compact-messages as `soft_budget`/`hard_budget` params; chars/4 fallback when no tokenizer; tests.py NameErrors (CHAR_SOFT/CHAR_HARD) fixed to token assertions; round-1 attempt (360e998/d9e2c66/6bc9b99) REJECTED — reviewer caught it skipped the core rename against SCOPE UPDATE v2
  - [Fail-Thread Routing (review_on_fail) + Double-Normalization Fix](./Todo/FailThreadRoutingReviewOnFailImplementation.md) **(IMPLEMENTED 2026-08-17, omniagent `4f8b1f9`)**: F0 empty `workflow_step` default fixed (double-normalization bug); `review_on_fail=true` wired into the fail matrix (tester F0 → review; only reviewer can send blocked; non-reviewer blocked/invalid/retry-limit → review)
  - [Wiki Data Source + Wiki Skill](./Todo/WikiSourceSkillImplementation.md) **(IMPLEMENTED 2026-08-19, omni-stack `9c86468` + omni-deployer `adb72f3` GROUP 45)**: resolve the 7-day-usage wiki finding (no wiki tool called) — NO dedicated wiki plugin exists (wiki = data source: profile wiki dir + `search_wiki` tool from the search plugin + optional off Qdrant vectorizer; 0 real `search_wiki` calls in 7 days); shipped the user-requested fix: new skill `skills/wiki.md` (Karpathy method + Obsidian format + filesystem-tool worked examples) + guidance convention #7 "check the wiki before asking the user"; vectorize_wiki stays off; no new plugin
  - [Dead Code Removal](./Todo/DeadCodeRemovalImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `614a3dd`)**: remove leftover dead code — `get_recent_summaries()` (src/db/summaries.rs:36, `#[allow(dead_code)]`, nothing calls it), `condense_messages()` (src/agent/helpers.rs:594, test-only legacy condenser, main loop uses compact tool + prune_old_tool_results), stale `old_message_char_budget` refs (after budget-unification task), plus a general sweep (other `#[allow(dead_code)]`, clippy-unused, settings whitelist keys) — deletion-only, no behavior change
  - [Compact + Prune + Budget → Prompt Plugin Only](./Todo/CompactPrunePluginOwnershipImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `e8239a0` + omni-deployer `0553cbc`/`f76d74f` + omni-stack `fde68b7`)**: architecture fix — context management is the prompt plugin's job; remove `prune_old_tool_results` (Layer 3, main_loop.rs:843) from core; **budgets are GLOBAL SETTINGS in omniagent (`prompt_token_budget_hard/soft`, AgentConfig token_budget_* from task 12) and compact-messages RECEIVES them as PARAMS (soft_budget/hard_budget) — the prompt plugin has NO budget config and stays agnostic of models.yml (user decision v2 2026-08-19)**; tool-result pruning moves INTO compact-messages (ChatMessage already carries tool_call_id/name — tool results identifiable; interface change allowed + documented); a custom prompt plugin can do context management completely differently as long as the interface stays the same; no core backstop re-added. **LLM CACHE (user 2026-08-19): ≥95% hit ratio** — stable-prefix invariant (never modify/reword/reorder pre-tail messages; never rewrite surviving tail messages — that increases misses; user-corrected), frozen byte-identical summary block (absorbs CacheFriendlyCompactionImplementation.md), miss-token minimization via AGENT-SIDE WINDOWING (skills/templates teach smaller windows + filesystem_read offset/limit paging + safe grep/rg content-search tool — NO black-magic source truncation; user-refined), drain-region compaction into the frozen summary (meaning-preserving excerpts), threshold-gated compaction (called every iteration, compacts only over hard budget — null-contract no-op path must be byte-identical), verify no mid-context changing upserts; baseline 94.4% (7d) must IMPROVE
  - [Provider/Model Overrides via config/models.yml](./Todo/ModelOverridesConfigImplementation.md) **(IMPLEMENTED 2026-08-19, omniagent `cb6c092`+`cf311c3`, omni-dashboard `21a65bd`, omni-deployer `38175b3` GROUP 46)**: pure-definition file (NO new plugin, NO custom code) — top-level `providers`, each child = provider name with `plugin:` flag (use provider plugin vs builtin chat_completions/anthropic support), `models` array (replaces `default_model.allowed_values` in dashboard selectors), provider-level fields (api_mode, default_base_url, refresh_url, default_model, api_key with $env:/$secret:, supports_reasoning) that OVERRIDE the provider plugin when names match; per-model config (api_mode, supports_reasoning, token_budget_soft/hard, max_tokens, max_tokens_on_truncation) with **exact budget fallback (user 2026-08-19): model_config > provider(models.yml) > global settings (defaults soft 100000 / hard 500000), resolved by omniagent and passed as compact-messages params**; plugin-less providers still selectable on threads; new dashboard "Models" page (/models) after Providers modeled on the /channels page (appearance+functionality, models API instead of channels API, updates models.yml); **refresh buttons (providers page, channel model selector, /models) UPSERT models.yml** — `plugin: true` + `models: [fetched]` when absent, ONLY `models` updated when present, NEVER mutate the plugin (rework refresh_plugin_models src/plugins_yaml.rs:1746 to write models.yml; user refinement 2026-08-19); **Import on /models page mirrors the plugins import (reuse/extract shared code from plugin-import.ts)** — URL to a models.yml-like file, per-provider add/override/same actions (same-config shown + removable), Confirm & Execute merges via API only; API GET/PUT /api/models
  - [Fail Routing: Board Fallback for Workflow Resolution](./Todo/FailRoutingBoardFallbackImplementation.md) **(Phase 1 IMPLEMENTED 2026-08-20, omniagent `8e238d2`+`1bdcde0`, omni-stack `0720d81`; Phase 2 (channel identity at load) IMPLEMENTED + APPROVED 2026-08-21, omniagent `57e16da`+`45bc5c2`, GROUP 47 — see FieldResolutionDataLoadTimeImplementation.md)** — **resolve fallback fields ONCE at load — universal pattern (user principle 2026-08-19)** — ANY field with a fallback chain (kanban task workflow/channel/profile/plan/provider/model being ONE case; also channel fields, provider/model resolution, settings defaults — anywhere such fallbacks exist) MUST be resolved FIRST, in ALL cases, as close to where values are loaded as possible. Trigger: fail-thread routing reads raw `kanban_tasks.workflow_id`; board-based tasks (NULL, board=omnidev) → reviewer/tester fails block instead of rework (live: reviewer thread 51, kanban_history #127). Phased fix: Phase 1 kanban task resolution (shared `resolve_task_defaults`: task → board → channel/global; fail routing + thread creation + dispatch + redispatch + action context), Phase 2 channel fields (resolve_channel_identity at load), Phase 3 provider/model per-thread resolution, Phase 4 settings resolved-at-load snapshot; one resolver per domain, no per-consumer resolvers, display APIs keep raw fields (except kanban API since 57e16da — it returns resolved values); regression tests per domain; Reference/Field-Resolution.md documents the rule
  - [Resolve Fields With Fallback AT DATA-LOAD TIME (Phase 2)](./Todo/FieldResolutionDataLoadTimeImplementation.md) **(IMPLEMENTED + TESTED + APPROVED 2026-08-21, omniagent `57e16da`+`45bc5c2`, GROUP 47 live, task_18cd7ecb9817b677)**: loaders return RESOLVED data, never shallow values — `ResolvedChannelIdentity` + `resolve_channel_identity` (profile yml→default profile; provider yml→resolved profile provider→global default_provider; model yml→profile model only when channel doesn't pin provider→resolve_default_model) applied in `def_to_channel` on EVERY load (channels.yml fresh per call, NO boot-time cache — provider edit takes effect next thread, no restart; fixes root-cause mm-kanban→opencode-go edit ignored, threads kept deepseek); `task_row_to_entry` resolves kanban API list/get (no shallow board rows, NULL channel/workflow/profile); regression guard 83f461b (wf-test pins preserved)
  - [Dashboard UI/UX Fixes](./Todo/DashboardUiUxFixesImplementation.md) **(IMPLEMENTED + TESTED + APPROVED 2026-08-21, omni-dashboard `fb9c680`..`877a9e7` + rework `1bf1405` + omni-deployer `7e49bb8` GROUP 49 9/9; rework cycle: 1744 REJECT → 1746 rework → 1750/51 tester PASS → 1752/53 APPROVE)**: omni-dashboard 8-item UI/UX pass (task_18cd65247cfc4d9e) — DB page 502 (`search_database {sql}` instead of `query_database`), custom selects in board/workflow/hook modals, workflow option order/defaults (review_on_fail first), hook trigger count `type="tel"`, template selects (alphabetical all-profiles, resolve from profile), opaque red Cancel, plugin Remove always, explorer git box to bottom; custom-select helper `enhanceSelect`/`enhanceSelectElement` in src/lib/dropdown.ts (consumers: kanban.ts `wireBoardControls`, kanban-boards.ts, hooks-detail.ts); rework `1bf1405` = board selector `#kanban-board-select` custom select (Item 2 first bullet, the 1744 rejection); GROUP 49 = static source-check regression; hygiene: scratch push scripts keep getting committed (gitignore + grep for secrets before approve)

- **Reference/**
  - [Agent Guidance Architecture](./Reference/Agent-Guidance-Architecture.md): The 4-layer guidance model (memory → templates → skills → wiki), what goes where, and conventions
  - [Budget & Context Mechanics](./Reference/Budget-and-Context.md): Thread budget, compaction behavior (TOKEN budgets: hard 500000 / soft 100000, chars/4 fallback), prompt context blocks, filesystem_read paging
  - [DeepSeek Prefix-Cache Misses](./Reference/DeepSeek-Prefix-Cache.md): Why cached_tokens froze at 7,424 (system-role upserts break the cache key) and the user-role fix (omniagent 9c5bb60) — 6.9% → 90%+ hit rate
  - [Deployment Checklist](./Reference/Deployment-Checklist.md): How to deploy services correctly via compose
  - [Omni-Deployer](./Reference/Omni-Deployer.md): The deploy harness — deploy.py dev pipeline (verified exit 0), noop-only rule, omnistable safety invariant, schema facts that bite tests, test-fix patterns; **provider HTTP-429 failure mode** (3x provider error → thread failed pre-work, retry-after ~39-40h, re-run works) and **running the omnidev chain from the omnistable container** (env_file project targeting — `-p` unsupported — + wait-task for long steps), both verified 2026-08-22
  - [Container Mount Map](./Reference/Container-Mount-Map.md): Volume mount mapping between host and container (omni-stack → /opt/omni, NOT /opt/data)
  - [Omniagent Mattermost Platform](./Reference/Omniagent-Mattermost-Platform.md): Mattermost platform architecture, setup, invariants, and recovery
  - [Verification & Review Requirements](./Reference/Verification-and-Review.md): Mandatory functional verification + review-before-commit for code deliverables (plugins, tools, services)
  - [Field Resolution](./Reference/Field-Resolution.md): resolve fallback fields ONCE at load (universal rule, user 2026-08-19) — kanban task defaults (resolve_task_defaults, Phase 1), channel identity at load (resolve_channel_identity / def_to_channel, Phase 2), provider/model thread identity, settings snapshot; kanban API returns resolved values; GROUP 47 regression
  - [Smoke-Test Threads](./Reference/Smoke-Test-Threads.md) **(NEW 2026-08-22)**: recognize automated smoke-test threads (noop provider / test-tool-caller model, rapid single-tool bursts, "Unknown tool: X" = toolset not registered, NOT a bug) — skip them in maintenance; null-contract compact verification; threads 70-77 confirmed (memory_list-memories alternation 73/74-75 + real-agent math-tester variant 76/77); 429-failed kanban threads are NOT smoke — re-run the task
- **Log**
  - [log.md](./log.md): Change log

## Key Facts

- You have NO shell/terminal tool. All operations go through MCP tools.
- Docker operations: `docker_compose` MCP tool only.
- File operations: `filesystem_*` MCP tools only. `filesystem_read` supports
  offset/limit char-based paging; `filesystem_search` matches file names only.
- Port checking via `fetch_fetch` is unreliable from inside the container — use `docker_compose ps`.
- Container mount map: `/opt/workspace` == `/opt/workspace`; omni-stack == `/opt/omni`;
  omniagent == `/app`.
