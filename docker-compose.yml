name: omni

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      DATABASE_URL: postgres://omniagent:***@postgres:5432/omniagent
      POSTGRES_DB: omniagent
      POSTGRES_USER: omniagent
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-omniagent} -d ${POSTGRES_DB:-omniagent}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  qdrant:
    image: qdrant/qdrant:v1.18.2
    volumes:
      - qdrant_data:/qdrant/storage
    expose:
      - "6333"

  omniagent:
    image: omniagent-dev:latest
    build:
      dockerfile: Dockerfile.dev
      context: /opt/workspace/omniagent
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    environment:
      DATABASE_URL: ${DATABASE_URL}
      POSTGRES_DB: omniagent
      POSTGRES_USER: omniagent
      QDRANT_URL: http://qdrant:6333
      HOST: 0.0.0.0
      PORT: 8080
      WORKSPACE_DIR: /opt/workspace/omni-stack
      HINDSIGHT_URL: http://hindsight:8888
      OMNI_DIR: /opt/omni
      DEFAULT_PROFILE: omni
    volumes:
      - /opt/workspace/omniagent:/app:rw
      - omniagent-target:/app/target
      - /opt/workspace/omni-stack:/opt/omni:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - /opt/workspace:/opt/workspace:rw
    expose:
      - "8080"
    ports:
      - "8080:8080"
    user: root
    extra_hosts:
      - "host.docker.internal:host-gateway"

  dashboard:
    image: ${DASHBOARD_IMAGE:-ghcr.io/nexuslbs/omni-dashboard:latest}
    ports:
      - "12346:3001"
    environment:
      PORT: "3001"
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGUSER: omniagent
      PGHOST: postgres
      OMNI_DIR: /opt/omni
    volumes:
      - /opt/workspace/omni-stack/:/opt/omni/:ro
      - /opt/workspace/omni-stack/user/:/opt/omni/user:rw
      - /opt/workspace/omni-stack/profiles/:/opt/omni/profiles:rw

  # ── Toolbox — always-running utility with backup/cron + dev tools ───
  toolbox:
    image: ${TOOLBOX_IMAGE:-ghcr.io/nexuslbs/omni-stack-toolbox:latest}
    build:
      context: ./toolbox
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: ${POSTGRES_USER:-omniagent}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGDATABASE: ${POSTGRES_DB:-omniagent}
    volumes:
      - ./:/opt/omni-stack:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - omni-toolbox-cron:/var/log

  # ── Mattermost ──────────────────────────────────────────────────────
  mattermost-db:
    image: postgres:16.14
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${MM_POSTGRES_USER:-mmuser}
      POSTGRES_PASSWORD: ${MM_POSTGRES_PASSWORD}
      POSTGRES_DB: mattermost
    volumes:
      - mm-db:/var/lib/postgresql/data:rw
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mmuser -d mattermost"]
      interval: 10s
      timeout: 5s
      retries: 5

  mattermost:
    image: mattermost/mattermost-team-edition:10.11.20
    restart: unless-stopped
    depends_on:
      mattermost-db:
        condition: service_healthy
    expose:
      - "8065"
    ports:
      - "12349:8065"
    environment:
      MM_SQLSETTINGS_DRIVERNAME: postgres
      MM_SQLSETTINGS_DATASOURCE: postgres://mmuser:${MM_POSTGRES_PASSWORD}@mattermost-db:5432/mattermost?sslmode=disable&connect_timeout=10
      MM_SERVICESETTINGS_SITEURL: https://hermes-app-5.nexuslbs.org
      MM_SERVICESETTINGS_ENABLEBOTACCOUNTCREATION: true
      MM_SERVICESETTINGS_ENABLEUSERACCESSTOKENS: true
    volumes:
      - mm-config:/mattermost/config:rw
      - mm-data:/mattermost/data:rw
      - mm-logs:/mattermost/logs:rw
      - mm-plugins:/mattermost/plugins:rw

  # hindsight — persistent memory provider
  hindsight:
    image: ghcr.io/vectorize-io/hindsight:0.7.2
    restart: unless-stopped
    expose:
      - "8888"
      - "9999"
    volumes:
      - hindsight-data:/home/hindsight/.pg0
    mem_limit: 2G
    logging:
      driver: journald

volumes:
  postgres_data:
  qdrant_data:
  hindsight-data:
  omniagent-target:
  mm-db:
  mm-config:
  mm-data:
  mm-logs:
  mm-plugins:
  omni-toolbox-cron:
