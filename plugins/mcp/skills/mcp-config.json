{
  "servers": [
    {
      "name": "skills",
      "transport": "stdio",
      "command": "/app/target/release/mcp-server-skills",
      "args": [],
      "timeout_secs": 30,
      "max_retries": 2,
      "allowed_tools": [
        "*"
      ],
      "env": {
        "OMNI_DATA_DIR": "${OMNI_DATA_DIR:-/opt/data}",
        "RUST_LOG": "info"
      }
    }
  ]
}