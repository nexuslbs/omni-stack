tools:
  # ── Bundled MCP servers (from plugins/mcp/ in omni-stack) ──
  cron:
    builtin: true
    enabled: true
  docker:
    builtin: true
    enabled: true
  fetch:
    builtin: true
    enabled: true
  filesystem:
    builtin: true
    enabled: true
  git:
    builtin: true
    enabled: true
  kanban:
    builtin: true
    enabled: true
  memory:
    builtin: true
    enabled: true
  metrics:
    builtin: true
    enabled: true
  plugin-manager:
    builtin: true
    enabled: true
  query:
    builtin: true
    enabled: true
  search:
    builtin: true
    enabled: true
  skills:
    builtin: true
    enabled: true
  subtasks:
    builtin: true
    enabled: true

  # ── Built-in action tools (kanban_dispatcher, hindsight_populator, relevance_indexer, setup_knowledge_pipeline) ──
  actions:
    builtin: true
    enabled: true

  # ── Hindsight memory server — recall, retain, reflect ──
  hindsight:
    enabled: true

  # ── Test tools (for development and testing) ──
  test-python-tool:
    builtin: true
    enabled: true
  test-rust-tool:
    builtin: true
    enabled: true
  test-rust-tool-2:
    enabled: false
  test-js-tool:
    builtin: true
    enabled: true

providers: null
platforms: null
